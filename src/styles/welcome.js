import { Dimensions } from 'react-native'
import styled from 'styled-components/native'
import { mainBackgroundColor } from './_common'

export const Container = styled.View`
  flex: 1;
  width: 100%;
  background-color: ${mainBackgroundColor};
`

